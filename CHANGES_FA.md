# چرا ربات مدتی سیگنال نمی‌داد و یک حرکت بزرگ رو جا انداخت؟

## علت اصلی: زنجیره‌ی فیلترهای سختِ AND پشت‌سرهم
قبل از رسیدن به هر سیگنال، باید **همزمان** همه‌ی این‌ها true می‌شد:

1. جهت غالب از میان تایم‌فریم‌های ۱هفته/۱روز/۴ساعت (با رای مساوی)
2. هم‌جهتی EMA20/EMA50 **و** RSI>۵۰ (هر دو با هم، فیلتر سخت)
3. ADX ≥ ۲۰ (فیلتر سخت)
4. امتیاز نهایی ≥ ۷۰
5. تایید مدل AI ≥ ۵۰٪ (اگه مدل train شده باشه)

هر کدوم از این‌ها به‌تنهایی منطقی‌ان، ولی چون همه به‌صورت AND پشت‌سرهم بودن (نه بخشی از یک
سیستم امتیازدهی)، احتمال عبور از **همه‌شون با هم** خیلی پایین میاد - به‌خصوص توی بازار
رنج (که ADX اکثر وقت‌ها پایینه). این دقیقاً باعث «مدتی کلاً سیگنال نمی‌داد» می‌شه.

## علت دوم (مهم‌تر): رای‌گیری مساوی بین تایم‌فریم‌ها -> جا موندن از حرکت‌های بزرگ
جهت معامله فقط از رای اکثریت بین ۱هفته/۱روز/۴ساعت (با وزن مساوی) تعیین می‌شد. وقتی یه
حرکت بزرگ **تازه** شروع می‌شه، تایم‌فریم ۴ساعته زودتر از هفتگی/روزانه برمی‌گرده. مثلاً اگه
هفتگی و روزانه هنوز «نزولی» بودن ولی ۴ساعته به‌وضوح «صعودی» شده بود، رای اکثریت (۲ به ۱)
می‌شد «نزولی» -> ربات دنبال سیگنال شورت می‌گشت در حالی که قیمت داشت پامپ می‌کرد -> فیلتر
EMA/RSI برای شورت رد می‌شد -> **هیچ سیگنالی صادر نمی‌شد**، دقیقاً همون سناریوی «از یه سود
بزرگ جا موندیم».

## چه چیزی تغییر کرد

### ۱. `bot/signal_engine.py` + `bot/config.py` - رای‌گیری وزن‌دار تایم‌فریم‌ها
تایم‌فریم‌های نزدیک‌تر به الان (۴ساعته) وزن بیشتری در تشخیص جهت غالب دارن
(`TREND_TF_WEIGHTS = {"1w": 1, "1d": 2, "4h": 3}`), چون سریع‌تر به برگشت واقعی روند
واکنش نشون می‌دن. همچنین اگه ساختار BOS/CHOCH روی یه تایم‌فریم هنوز رویدادی نداشت،
به‌جای نادیده گرفتن کامل اون تایم‌فریم، از شیب EMA به‌عنوان تخمین کمکی استفاده می‌شه.

### ۲. `bot/scoring.py` - تبدیل فیلترهای سخت ADX و EMA/RSI به امتیاز نرم
یه امتیاز جدید `momentum` (وزن ۱۰ از ۱۰۰) اضافه شد که ترکیبیه از قدرت ADX (نسبت به آستانه‌ی
هدف، نه یک خط قرمز) و هم‌جهتی EMA/RSI. دیگه این دو به‌تنهایی کل سیگنال رو رد نمی‌کنن؛ فقط
سیگنال‌های بی‌کیفیت‌تر امتیاز کمتری می‌گیرن. فقط زیر یک کف مطلق (`ADX_HARD_FLOOR=10`, یعنی
بازار واقعاً کاملاً بی‌روند) رد کامل می‌شه، نه زیر ۲۰.

وزن‌های امتیازدهی بازتوزیع شدن تا جمعشون هنوز ۱۰۰ بمونه (`trend`: ۲۷→۲۲, `news`: ۱۰→۵,
`momentum`: جدید ۱۰).

### ۳. آستانه‌ی ارسال کمی پایین‌تر اومد
`SCORE_MIN_SEND` از ۷۰ به ۶۵ کاهش پیدا کرد (چون momentum/order_block دیگه فیلتر سخت جدا
نیستن، منطقی بود آستانه‌ی امتیاز کمی نرم‌تر بشه). این مقدار، `SCORE_GOOD`, `SCORE_GOLDEN`
و `ADX_HARD_FLOOR` همه از Environment Variable قابل تنظیمن - اگه دوباره سخت‌گیرانه‌تر
یا سست‌تر می‌خوای، نیازی به تغییر کد نیست:
```
SCORE_MIN_SEND=70
ADX_HARD_FLOOR=15
ADX_MIN_TREND_STRENGTH=25
```

### ۴. `app.py` - هشدار خودکار «سکوت» در تلگرام
اگه بیش از `SILENCE_ALERT_HOURS` ساعت (پیش‌فرض ۶) هیچ سیگنالی ارسال نشه، یه پیام تشخیصی
خودکار به تلگرام می‌فرسته که خلاصه‌ی دلایل رد شدن سیگنال‌ها رو نشون می‌ده (دقیقاً همون
`diagnostic_rejection_reasons` که قبلاً فقط توی JSON اندپوینت `/api/scan` بود و باید دستی
چک می‌کردی). این هشدار حداکثر هر `SILENCE_ALERT_HOURS` یه‌بار فرستاده می‌شه (اسپم نمی‌شه).

## نکته‌ی مهم درباره‌ی مدل AI
فایل `model.joblib` توی پروژه‌ی آپلودی وجود نداشت (یعنی هنوز هیچ مدلی train نشده و ربات از
fallback قانون‌محور استفاده می‌کنه - که با همین امتیاز ۰-۱۰۰ کار می‌کنه). اگه ورک‌فلوی
`auto-retrain.yml` رو فعال کردی و مدل واقعی train شده، حتماً بعد از این تغییرات یه بار
`train_model.py` رو دوباره اجرا کن، چون فیچرهای ورودی مدل تغییر نکردن ولی توزیع سیگنال‌های
واقعی (که پایه‌ی retrain زنده‌ست) عوض می‌شه.

## پیشنهاد بعدی (اختیاری، پیاده‌سازی نشده)
اگه هنوز حس می‌کنی زیادی محتاطه، می‌تونی این‌ها رو هم اضافه کنیم:
- مسیر جدا برای «سیگنال مومنتومی/کانتر-ترند» با آستانه‌ی امتیاز بالاتر (مثلاً ۸۰) که وقتی
  روند بزرگ هنوز کامل تایید نشده ولی حجم+ADX+Breakout خیلی قوی‌ان، جداگانه ارسال بشه.
- گزارش «نزدیک به سیگنال» (near-miss, امتیاز بین ۵۵ تا ۶۵) در کنار هشدار سکوت، تا ببینی
  چقدر به آستانه نزدیک بودی.
